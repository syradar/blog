## Agent skills

### Issue tracker

Issues live in GitHub Issues (`github.com/syradar/blog`). See `docs/agents/issue-tracker.md`.

### Triage labels

Using default canonical label vocabulary. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo — one `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
